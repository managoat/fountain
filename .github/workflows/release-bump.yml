name: Release bump

# Cuts a release in two halves, because main only takes pull requests.
#
# 1. "Run workflow" (Actions tab → "Release bump"; defaults to `patch`)
#    bumps the version in both mix.exs files,
#    and the six self-host image pins, rolls the changelog.d/ fragments into
#    CHANGELOG.md, and opens the PR from a `release/vX.Y.Z` branch. CI runs
#    on it like any other PR; the compose boot check knows the pinned image
#    does not exist yet and skips, and the changelog guard knows the release
#    branch is the one that edits CHANGELOG.md.
# 2. Queueing that PR (`gh pr merge --squash --auto`, like every PR here)
#    triggers the `tag` job below once the queue merges it. The trigger is
#    `pull_request: [closed]`, which the queue fires the same way a direct
#    merge did. It tags the squash commit and
#    dispatches release.yml to build the CLI binaries, publish the
#    versioned image, boot it, and create the GitHub Release.
#
# This used to be one job that committed straight to main, tagged and
# pushed. The Main Protection ruleset (pull requests only, no bypass actors,
# and a user account cannot grant the Actions app one) rejects that push,
# which is how two dispatches at v0.15.0 failed before the tag step (#1329).
#
# The PR is opened with RELEASE_BUMP_TOKEN, a personal access token with
# write access to this repository's contents and pull requests (a classic
# PAT with `repo`, or a fine-grained one with Contents and Pull requests
# read/write), and not with GITHUB_TOKEN: a branch pushed and a PR opened by
# GITHUB_TOKEN trigger no workflows, so the bump PR would arrive with no
# checks at all. The tag half needs no such token; a tag pushed by
# GITHUB_TOKEN is fine, and release.yml is dispatched explicitly because the
# same rule suppresses its `push: tags:` trigger.

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Which segment to bump"
        required: true
        default: "patch"
        type: choice
        options:
          - patch
          - minor
          - major
  pull_request:
    types: [closed]
    branches: [main]

permissions:
  contents: read

jobs:
  open-pr:
    name: Bump version and open the release PR
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5

    permissions:
      contents: read

    steps:
      - name: Require RELEASE_BUMP_TOKEN
        env:
          TOKEN: ${{ secrets.RELEASE_BUMP_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${TOKEN}" ]; then
            echo "RELEASE_BUMP_TOKEN is not set." >&2
            echo "Add a PAT with write access to this repository's contents and pull requests as that secret." >&2
            echo "GITHUB_TOKEN cannot open this PR: a PR it opens gets no CI run." >&2
            exit 1
          fi

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4
        with:
          # Full history so the tag and branch checks below see what exists.
          fetch-depth: 0
          token: ${{ secrets.RELEASE_BUMP_TOKEN }}

      - name: Compute next version
        id: ver
        run: |
          set -euo pipefail
          current=$(grep -oE 'version: "[0-9]+\.[0-9]+\.[0-9]+"' mix.exs \
            | head -1 \
            | sed -E 's/version: "([0-9]+\.[0-9]+\.[0-9]+)"/\1/')
          echo "current=$current"
          IFS='.' read -r major minor patch <<<"$current"
          case "${{ inputs.bump }}" in
            patch) patch=$((patch + 1)) ;;
            minor) minor=$((minor + 1)); patch=0 ;;
            major) major=$((major + 1)); minor=0; patch=0 ;;
            *) echo "unknown bump: ${{ inputs.bump }}" >&2; exit 1 ;;
          esac
          next="${major}.${minor}.${patch}"
          tag="v${next}"
          branch="release/${tag}"
          echo "next=$next"
          {
            echo "next=$next"
            echo "tag=$tag"
            echo "branch=$branch"
            echo "current=$current"
          } >> "$GITHUB_OUTPUT"
          if git rev-parse "refs/tags/${tag}" >/dev/null 2>&1; then
            echo "tag ${tag} already exists" >&2
            exit 1
          fi
          if git rev-parse "refs/remotes/origin/${branch}" >/dev/null 2>&1; then
            echo "branch ${branch} already exists; merge or delete the open release PR first" >&2
            exit 1
          fi

      - name: Roll the changelog
        env:
          NEXT: ${{ steps.ver.outputs.next }}
          BUMP: ${{ inputs.bump }}
        run: |
          set -euo pipefail
          # The release PR is the one place CHANGELOG.md changes. The roll
          # folds [Unreleased] and every changelog.d/ fragment into a dated
          # ## [NEXT] section and deletes the fragments (scripts/changelog.py,
          # changelog.d/README.md). Two guards it keeps from the hand-rolled
          # days: v0.3.0 shipped with its upgrade notes stranded under
          # [Unreleased] (#318), so the roll refuses an empty release; and
          # pre-1.0 a minor may break, so a minor or major refuses without an
          # "Upgrade notes" entry, which the CREDITS_ENABLED flip nearly
          # shipped without. "- None." is fine when true; explicit beats
          # absent.
          flags=()
          if [ "${BUMP}" != "patch" ]; then flags+=(--require-upgrade-notes); fi
          python3 scripts/changelog.py release --version "${NEXT}" --date "$(date -u +%F)" "${flags[@]}"
          git status --short -- CHANGELOG.md changelog.d

      - name: Update server versions
        run: |
          set -euo pipefail
          current='${{ steps.ver.outputs.current }}'
          next='${{ steps.ver.outputs.next }}'
          # Swift has its own version and publisher; a server bump does not release it.
          sed -i -E "s/version: \"${current}\"/version: \"${next}\"/" mix.exs
          sed -i -E "s/version: \"${current}\"/version: \"${next}\"/" apps/fountain/mix.exs
          git diff --stat

      - name: Update self-host image pins
        run: |
          set -euo pipefail
          current='v${{ steps.ver.outputs.current }}'
          next='${{ steps.ver.outputs.tag }}'
          # The quick-start pins sat at v0.3.0 through two releases, handing
          # newcomers an image from before the in-app first-login flow
          # (#478). Bump them in the release commit so the tag's own tree
          # pins the tag. ReleasePinTest keeps this list and the files in
          # lockstep between releases.
          pins='docker-compose.yml .env.compose.example render.yaml fly.toml deploy/k8s/kustomization.yaml deploy/k8s/deployment.yaml'
          for f in ${pins}; do
            sed -i "s/${current}/${next}/g" "$f"
            # sed exits 0 whether or not it matched; a pin that no longer
            # matches ${current} must abort the release, not ship stale.
            if ! grep -qF "${next}" "$f" || grep -qF "${current}" "$f"; then
              echo "pin bump failed in ${f}: expected ${current} -> ${next}" >&2
              exit 1
            fi
          done
          git diff --stat

      - name: Commit, push the branch, open the PR
        env:
          GH_TOKEN: ${{ secrets.RELEASE_BUMP_TOKEN }}
          TAG: ${{ steps.ver.outputs.tag }}
          BRANCH: ${{ steps.ver.outputs.branch }}
        run: |
          set -euo pipefail
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b "${BRANCH}"
          git add mix.exs apps/fountain/mix.exs \
            docker-compose.yml .env.compose.example render.yaml fly.toml \
            deploy/k8s/kustomization.yaml deploy/k8s/deployment.yaml \
            CHANGELOG.md changelog.d
          # No [skip ci]: the PR is meant to run CI, and a main push whose
          # tree a PR run already tested is skipped by ci.yml on its own.
          # -s: workflow-checks' DCO gate (#2290) judges this PR's commits
          # like any other; the bot identity configured above signs it off.
          git commit -s -m "chore(release): ${TAG}"
          git push origin "${BRANCH}"
          cat > "${RUNNER_TEMP}/body.md" <<EOF
          Version bump for ${TAG}: both \`mix.exs\` files, the six self-host image pins, and the changelog roll (\`[Unreleased]\` plus every \`changelog.d/\` fragment into \`## [${TAG#v}]\`).

          Merging this PR tags the squash commit \`${TAG}\` and dispatches \`release.yml\`, which builds the CLI binaries, publishes \`ghcr.io/managoat/fountain:${TAG}\`, boots it through the documented quick start and creates the GitHub Release.

          "Compose boots the pinned image" skips on this PR because the pinned image does not exist until the release runs; the same check runs on the tag instead.
          EOF
          gh pr create \
            --base main \
            --head "${BRANCH}" \
            --title "chore(release): ${TAG}" \
            --body-file "${RUNNER_TEMP}/body.md"

  tag:
    name: Tag the merged release PR and dispatch release.yml
    if: >-
      ${{ github.event_name == 'pull_request'
          && github.event.pull_request.merged == true
          && github.event.pull_request.head.repo.full_name == github.repository
          && startsWith(github.event.pull_request.head.ref, 'release/v') }}
    runs-on: ubuntu-latest
    timeout-minutes: 5

    permissions:
      # Push the tag.
      contents: write
      # Dispatch release.yml: a tag pushed by GITHUB_TOKEN does not fire its
      # `push: tags:` trigger (GitHub's anti-recursion rule).
      actions: write

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4
        with:
          # The squash commit on main, not the PR's merge ref (which is gone
          # once the PR closes). Full history so existing tags are visible.
          ref: ${{ github.event.pull_request.merge_commit_sha }}
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve the tag from the merged tree
        id: ver
        env:
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
          MERGE_SHA: ${{ github.event.pull_request.merge_commit_sha }}
        run: |
          set -euo pipefail
          # The version the merged tree carries is the truth; the branch name
          # is a cross-check. A hand-opened bump PR (v0.15.0 was one) takes
          # this path too, as long as its branch is release/vX.Y.Z.
          version=$(grep -oE 'version: "[0-9]+\.[0-9]+\.[0-9]+"' mix.exs \
            | head -1 \
            | sed -E 's/version: "([0-9]+\.[0-9]+\.[0-9]+)"/\1/')
          tag="v${version}"
          git fetch --quiet origin main
          if [ "${HEAD_REF}" != "release/${tag}" ]; then
            echo "merged tree is at ${tag} but the PR branch is ${HEAD_REF}; not tagging" >&2
            exit 1
          fi
          if ! git merge-base --is-ancestor "${MERGE_SHA}" origin/main; then
            echo "${MERGE_SHA} is not on main; not tagging" >&2
            exit 1
          fi
          if existing="$(git rev-parse -q --verify "refs/tags/${tag}^{commit}")"; then
            if [ "${existing}" = "${MERGE_SHA}" ]; then
              echo "${tag} already points at ${MERGE_SHA}; nothing to push"
              echo "push=false" >> "$GITHUB_OUTPUT"
            else
              echo "${tag} already exists at ${existing}, not at the merge commit ${MERGE_SHA}" >&2
              exit 1
            fi
          else
            echo "push=true" >> "$GITHUB_OUTPUT"
          fi
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"

      - name: Tag and push
        if: ${{ steps.ver.outputs.push == 'true' }}
        env:
          TAG: ${{ steps.ver.outputs.tag }}
          MERGE_SHA: ${{ github.event.pull_request.merge_commit_sha }}
        run: |
          set -euo pipefail
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "${TAG}" -m "Release ${TAG}" "${MERGE_SHA}"
          git push origin "refs/tags/${TAG}"

      - name: Dispatch release.yml
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.ver.outputs.tag }}
        run: |
          set -euo pipefail
          # If the tag was pushed by hand with a personal token, release.yml
          # already fired on the push; a second run for the same tag would
          # publish the same image and fail at the release page. Only
          # dispatch when nothing is running or done for it.
          runs="$(gh run list --workflow release.yml --branch "${TAG}" --json databaseId --jq 'length')"
          if [ "${runs}" != "0" ]; then
            echo "release.yml already has ${runs} run(s) for ${TAG}; not dispatching again"
            exit 0
          fi
          gh workflow run release.yml --ref "${TAG}" -f tag="${TAG}"
          echo "Dispatched release.yml for ${TAG}"
