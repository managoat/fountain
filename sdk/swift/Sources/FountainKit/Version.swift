/// The Fountain server release this client shipped with. Stamped by
/// `.github/workflows/release-bump.yml` alongside `fountainSDKVersion`, and
/// checked against `mix.exs` in CI — the two Swift clients release together.
public let fountainKitVersion = "0.18.0"
